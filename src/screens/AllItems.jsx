import { StyleSheet, Text, View } from 'react-native';

const AllItems = () => {
  return (
    <View>
      <Text>AllItems</Text>
    </View>
  );
};

export default AllItems;

const styles = StyleSheet.create({});
